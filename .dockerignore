# Node modules — always reinstalled inside the image
node_modules
server/node_modules
web/node_modules

# Development build artefacts
web/dist

# Runtime data (populated at runtime via a Docker volume)
server/data

# Development-only files
.git
.gitignore
*.md
*.plan.md
demo_data.js
web/.vite
